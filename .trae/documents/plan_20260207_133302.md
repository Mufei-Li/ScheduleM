# Phase 2: Core Feature Implementation (Continued)

## Current Progress
- **Monorepo**: Stable.
- **Core**: Stable and utilized by Web.
- **Services**:
  - LLMService: TS Converted.
  - PDFService: Implemented (API strategy).
  - ExcelService: Implemented (SheetJS).
- **Mobile**:
  - Skeleton: Ready.
  - Services: Integrated (LLM, PDF, Excel ready to use).

## Next Steps

### 1. Export Modules (T4)
- **T4-1: ICS Export Extraction**: Migrate `script.js` ICS logic to `packages/core/src/export/exportIcs.ts`.
- **T4-2: HTML Export Extraction**: Migrate `script.js` HTML logic to `packages/core/src/export/exportHtml.ts`.

### 2. Mobile UI Implementation (T6)
- **T6-2: Mobile Calendar Screen**: Implement the visual calendar grid.
- **T6-3: Mobile Course Logic**: Hook up the services to the UI (Import -> Parse -> Display).
- **T6-4: Mobile Offline Storage**: Persist data.

## Immediate Action
- Proceed with **T4-1** and **T4-2** to complete the logic extraction phase, ensuring all core business logic (Import, Process, Export) is in `packages/core` or `packages/services`.
